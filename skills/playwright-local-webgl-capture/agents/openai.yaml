interface:
  display_name: "Local WebGL Capture"
  short_description: "Fast local WebGL screenshot capture"
  default_prompt: "Use $playwright-local-webgl-capture to open my local app through the automation capture route, wait for the paused ready state, capture a screenshot quickly, and return the timings plus browser logs."
